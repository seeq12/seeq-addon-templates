formulaPackage_prompts:
  type: str
  help: "\n\n ******* FormulaPackage element configuration *******\n This element does not required additional configuration."
  default: Press enter to continue
  when: "{{ 'FormulaPackage' in elements_to_include }}"
